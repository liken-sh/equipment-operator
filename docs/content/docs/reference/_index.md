---
title: Reference
weight: 20
---

The reference lists every field of the `Receiver` and `CECBus`
resources, the two topics the operator writes on the media bus, and
the metrics it serves. The field pages are generated from the CRDs
in the repository's `deploy/` directory, so the descriptions there
are the ones `kubectl explain` shows.
