---
title: Reference
weight: 20
---

The reference lists every field of the `Receiver` resource. It is
generated from the CRD in the repository's `deploy/` directory, so
the descriptions here are the ones `kubectl explain` shows.
