---
title: Reference
weight: 20
---

The reference lists every field of the `Receiver` resource, the
two topics the operator writes on the media bus, and the metrics it
serves. The field page
is generated from the CRD in the repository's `deploy/` directory,
so the descriptions there are the ones `kubectl explain` shows.
